# Copy this file to .env and adjust as needed.

# Do not commit real secrets to version control.

DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/sprint_intelligence

PROJECT_NAME=Sprint Management Platform

ENVIRONMENT=local

# Used by docker-compose to initialize the Postgres container.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=sprint_intelligence
