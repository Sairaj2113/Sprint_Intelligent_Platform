"""
Pydantic request/response schemas.

Intentionally left empty in this step — this project only builds the
backend foundation (models, DB connectivity, health checks). Request and
response schemas for full CRUD APIs will be added in a later step.
"""
